# ============================================================
#  Autostart
# ============================================================
# Export Wayland session env to dbus/systemd so portals (screenshare,
# file pickers), keyring, and notifications work reliably.
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP HYPRLAND_INSTANCE_SIGNATURE

exec-once = hyprctl setcursor catppuccin-mocha-dark-cursors 24
exec-once = systemctl --user start hyprpolkitagent

# Bar + system controls + notifications (replaces waybar + swaync)
exec-once = hyprpanel

# Wallpaper (hyprpaper's conf-apply races on startup; set it via IPC after)
exec-once = hyprpaper
exec-once = ~/.local/bin/set-wallpaper.sh

# Idle: hypridle only on battery (AC = no idle).
# Backlight minimum is enforced by the brightnessctl shim in /usr/local/bin.
exec-once = ~/.local/bin/idle-on-battery.sh

# Clipboard: cursor-clip daemon (monitors + persists history, text + images)
exec-once = cursor-clip --daemon

# OSD popup on keyboard-layout change
exec-once = ~/.local/bin/kb-layout-osd.py

# Network / power applets handled by HyprPanel; nothing else needed here